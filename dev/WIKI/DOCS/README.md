# DOCS Wiki

This directory routes documentation work inside the WIKI.

The former `docs/v3` corpus now lives at `dev/WIKI/DOCS/v3`. Root-level public docs may be absent when they are intentionally retired; do not recreate them unless the user asks.

Start with `docs-map.md`.
