# PDF Header And Footer Contract

This is the MAP contract for PDF marginalia: the first-page header and last-page footer generated by Owen Graphite.

This surface has already burned a lot of release time because Chromium print/PDF support is narrower than normal browser layout. Treat this area as a fragile print-only system, not as general Reading View decoration.

## Source Owners

- `src/features/40-style-settings.css`: Style Settings metadata and user-facing controls.
- `src/tokens/00-light-tokens.css`: default PDF marginalia tokens.
- `src/tokens/01-dark-tokens.css`: dark-mode PDF marginalia token overrides.
- `src/features/41-feature-presets.css`: the only owner of `body.ogd-pdf-header-enabled` and `body.ogd-pdf-footer-enabled` anchor pseudo rules.
- `src/features/43-print-base.css`: the only owner of base `@page` size/margin rules.

## Hard Rules

- PDF header/footer paint rules must live inside `@media print`.
- `body.ogd-pdf-header-enabled` and `body.ogd-pdf-footer-enabled` selectors are owned by `src/features/41-feature-presets.css` only.
- Header must use one anchor pseudo: `.markdown-rendered::before`.
- Footer must use one anchor pseudo: `.markdown-rendered > :last-child::after`.
- Footer must reserve space on `.markdown-rendered > :last-child` with a millimeter-based `margin-bottom`.
- Generated text must use single-line `content: var(--ogd-pdf-header-text, "")` or `content: var(--ogd-pdf-footer-text, "")`.
- Quick presets, label style, compact sizing, and header alignment must be implemented by CSS variables/classes only; do not add more pseudo anchors.
- Anchor pseudos must use `position: absolute`, `pointer-events: none`, `white-space: nowrap`, and exact print color adjustment.
- `@page` rules belong in `src/features/43-print-base.css` and must be nested under `@media print`.

## Forbidden Patterns

- Do not use `@page` margin boxes such as `@top-center` or `@bottom-right`.
- Do not use `string-set`, CSS `content()` function, `::first-line`, multiline generated content (`\A`), list-item tricks, or `::marker` tricks for marginalia.
- Do not add `!important`, `position: fixed`, viewport units, or backdrop filters to PDF marginalia rules.
- Do not target Live Preview / CM6 selectors from PDF marginalia rules.
- Do not make the feature visible in Reading View; it is print-only unless a new contract and visual test plan are added first.
- Do not add new footer positions without a PDF sample and pagination regression check; the stable footer position is bottom center.

## Required Validation

Run these before release after touching any PDF, print, or report-output source:

```powershell
.\.venv\Scripts\python.exe dev\scripts\bundle_v3.py --check
.\.venv\Scripts\python.exe dev\scripts\audit_pdf_header_footer.py
.\.venv\Scripts\python.exe dev\scripts\build_src_map.py
```

Any MAP finding in the `pdf-header-footer-sensitive` category must be reviewed against this contract before release.